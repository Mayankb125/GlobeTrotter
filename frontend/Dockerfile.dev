FROM node:20-alpine

WORKDIR /app

# Install dependencies first (for better caching)
COPY package*.json ./
RUN npm install

# Copy source code (in docker-compose, this is overridden by volume mounts)
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start command
CMD ["npm", "run", "dev"]
